/**
 * Created by REDWork on 25.05.2016.
 */
function inc(val)
{
    val++;
}
var location1;
var location2;
var location3;

var guess;
var hits = 0;
var guesses = 0;
//
//var isSunk = false;
////Todo:
////while (prompt(Math.random())!="a"){}
//inc(hits)
//alert(hits);

var textStiler
{
     function SetRedColor(id){
        var textField=document.getElementById(id);
         if(textField!=null)
             textField.setAttribute("class","redtext");
    }
}
function SetRedColorFunc(){
    var textField=document.getElementById("mainHead");
    if(textField!=null)
    {
        if(textField.getAttribute("class")=="redText")
            textField.setAttribute("class","blackText");
        else
            textField.setAttribute("class","redText");
    }
}
